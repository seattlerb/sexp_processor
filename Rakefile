# -*- ruby -*-

require 'rubygems'
require 'hoe'

Hoe.add_include_dirs "lib"

require './lib/sexp_processor.rb'

Hoe.new('SexpProcessor', SexpProcessor::VERSION) do |sexp|
  sexp.rubyforge_name = 'parsetree'
  sexp.developer('Ryan Davis', 'ryand-ruby@zenspider.com')
end

# vim: syntax=Ruby
